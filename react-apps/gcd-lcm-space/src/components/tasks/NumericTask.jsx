import {useEffect,useState} from 'react';
export function NumericTask({task,disabled,onSubmit}){const[value,setValue]=useState('');useEffect(()=>setValue(''),[task.id]);const valid=value.trim()!==''&&Number.isInteger(Number(value));return <form className="numeric-form" onSubmit={e=>{e.preventDefault();if(valid&&!disabled)onSubmit(Number(value));}}><input inputMode="numeric" pattern="[0-9]*" value={value} onChange={e=>setValue(e.target.value)} disabled={disabled} aria-label="Ответ" placeholder="Введи ответ"/><button className="primary-button" type="submit" disabled={disabled||!valid}>Проверить</button></form>;}
