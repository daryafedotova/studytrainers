import {useEffect,useState} from 'react';
import {motion} from 'framer-motion';
export function MultiSelectTask({task,disabled,onSubmit}){const[selected,setSelected]=useState([]);useEffect(()=>setSelected([]),[task.id]);function toggle(index){setSelected(v=>v.includes(index)?v.filter(x=>x!==index):[...v,index]);}const answer=selected.map(i=>task.data.options[i]);return <><div className="answer-grid compact-grid">{task.data.options.map((option,index)=><motion.button key={`${option}-${index}`} type="button" disabled={disabled} className={`answer-button ${selected.includes(index)?'selected':''}`} onClick={()=>toggle(index)} aria-pressed={selected.includes(index)} whileHover={!disabled?{y:-3,scale:1.012}:{}} whileTap={!disabled?{y:2}:{}}>{option}</motion.button>)}</div><button className="primary-button submit-answer" type="button" disabled={disabled||selected.length===0} onClick={()=>onSubmit(answer)}>Проверить выбор</button></>;}
